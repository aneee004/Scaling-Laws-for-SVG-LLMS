# Smoke-test overrides: tiny budget for verifying the train loop end-to-end.
training:
  max_steps: 5
  batch_tokens: 8192
  micro_batch_size: 4
  eval_interval: 2
  save_interval: 1000
  checkpoint_dir: "checkpoints_smoke"
